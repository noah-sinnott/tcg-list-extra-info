# Vite environment variables must be prefixed with VITE_
# These optimizations help with faster builds

# API URL (can be accessed via import.meta.env.VITE_API_URL)
VITE_API_URL=http://127.0.0.1:5000/api
